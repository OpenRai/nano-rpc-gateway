# Development-only RPC configuration for docker-compose.dev.yml.
# Keep the native RPC reachable only from the Docker network/host mapping.
[rpc]
address = "::ffff:0.0.0.0"
port = 7076
enable_control = false
